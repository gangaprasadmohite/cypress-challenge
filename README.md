# cypress-challenge

Clone this repository.

`https://github.com/gangaprasadmohite/cypress-challenge.git`

Install all the dependecies with `npm install`.

Open cypress test runner using `npx cypress open`

Run the test.
